# Параметрическое моделирование компенсирующей упругой муфты со звёздочкой (ГОСТ 14084–76)

Это приложение позволяет автоматически создавать и визуализировать параметрическую сборку **компенсирующей упругой муфты со звёздочкой** по ГОСТ 14084–76.  
Разработано с использованием **Qt6** и геометрического ядра **OpenCASCADE**.  
Поддерживается выбор исполнения (для длинных/коротких концов валов), предпросмотр в реальном времени и экспорт 3D-моделей.

---

## 🔧 Зависимости

Для сборки требуются: **CMake**, **Git**, **GCC** (или Clang), **Ninja** и **Qt6**.

### Arch Linux / Manjaro
```bash
sudo pacman -S qt6-base qt6-tools gcc cmake ninja git
```

### Ubuntu / Debian
```bash
sudo apt install qt6-base-dev qt6-tools-dev libgl1-mesa-dev cmake ninja-build build-essential git
```

### Fedora
```bash
sudo dnf install qt6-qtbase-devel qt6-qttools-devel mesa-libGL-devel cmake gcc-c++ ninja-build git
```

> OpenCASCADE будет автоматически скомпилирован как Git-подмодуль — отдельная установка не требуется.

---

## 📥 1. Клонирование репозитория

```bash
git clone https://github.com/SankaKotik/qt_kursovik
cd qt_kursovik
git submodule update --init --recursive
```

> Подмодуль `occt` (OpenCASCADE) загружается автоматически.

---

## 🛠 2. Сборка проекта

```bash
mkdir build && cd build
cmake .. -G Ninja
ninja
```

> При первом запуске CMake автоматически соберёт OpenCASCADE из исходников — это может занять 5–15 минут в зависимости от системы.

---

## ▶️ 3. Запуск приложения

```bash
LD_LIBRARY_PATH=occt-install/lib ./QtKursovik
```

> Библиотеки OpenCASCADE устанавливаются локально в папку `occt-install`, поэтому важно указать `LD_LIBRARY_PATH`.

---

## 📚 Используемые стандарты

- **ГОСТ 14084–76** — Муфты упругие со звёздочкой  
- **ГОСТ 12080–66** — Концы валов цилиндрические

---

Если вы студент или разработчик CAD-систем — добро пожаловать к использованию и доработке!